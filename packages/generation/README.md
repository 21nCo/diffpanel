# @diffpanel/generation

Provider-neutral generation instructions, immutable generation-input
formatting, and output validation contracts for Diffpanel review generators.

```ts
import {
  generationContract,
  formatGenerationInput,
  validateGeneratedReview,
} from "@diffpanel/generation";
```

The package is browser-safe. Requires Node.js 22.13 or newer.
